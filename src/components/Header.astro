<header class="header">
  <div class="container">
    <a href="#hero" class="logo">
      <span class="first">Viktoriia</span>
      <span class="last">Buriak</span>
    </a>
    <nav class="nav">
      <a href="#skills" data-section="skills" data-i18n="nav.skills"></a>
      <a href="#projects" data-section="projects" data-i18n="nav.projects"></a>
      <a href="#contact" data-section="contact" data-i18n="nav.contact"></a>
    </nav>
    <div class="lang">
      <button data-lang="ua" onclick="i18next.changeLanguage('ua')">UA</button>
      <button data-lang="pl" onclick="i18next.changeLanguage('pl')">PL</button>
      <button data-lang="en" onclick="i18next.changeLanguage('en')">EN</button>
    </div>
  </div>
</header>
<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 20;
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--pink-soft);
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 1.5rem;
    display: flex;
    align-items: center;
  } /* Logo */
  .logo {
    text-decoration: none;
    font-size: 1.2rem;
    font-weight: 600;
    display: flex;
    gap: 0.3rem;
    align-items: baseline;
    cursor: pointer;
  }
  .logo .first {
    color: var(--text-main);
  }
  .logo .last {
    color: var(--pink-main);
    position: relative;
  } /* декоративний акцент */
  .logo .last::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -3px;
    width: 100%;
    height: 2px;
    background: var(--pink-main);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.25s ease;
  }
  .logo:hover .last::after {
    transform: scaleX(1);
  } /* легкий hover */
  .logo:hover .first {
    color: var(--pink-dark);
  } /* Nav */
  .nav {
    display: flex;
    gap: 0.8rem;
    margin-left: auto;
    margin-right: 2rem;
  }
  .nav a {
    padding: 0.45rem 0.9rem;
    text-decoration: none;
    font-size: 0.9rem;
    color: var(--pink-dark);
    border: 1px solid var(--pink-main);
    border-radius: 999px;
    transition: all 0.2s ease;
  }
  .nav a:hover {
    background: var(--pink-main);
    color: white;
  }
  .nav a.active {
    background: var(--pink-main);
    color: white;
  } /* Languages */
  .lang button {
    margin-left: 0.4rem;
    font-size: 0.8rem;
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
  }
  .lang button.active {
    color: var(--pink-main);
    font-weight: 600;
  }
</style>

