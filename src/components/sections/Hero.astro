<section class="hero" id="hero">
  <div class="container">
    <div class="content">
      <h1>
        <span data-i18n="hero.title"></span>
        <span class="accent" data-i18n="hero.subtitle"></span>
      </h1>

      <p data-i18n="hero.description"></p>
    </div>

    <div class="photo">
      <img src="public/me.jpg" alt="Viktoriia portrait" />
    </div>
  </div>
</section>

<style>
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  background: linear-gradient(
    120deg,
    var(--bg-main) 65%,
    var(--pink-soft) 35%
  );
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 4rem 1.5rem;
  display: grid;
  grid-template-columns: 2fr 1fr;
  align-items: center;
  gap: 3rem;
}

/* Text */
.content {
  animation: fadeInLeft 0.8s ease-out forwards;
}

h1 {
  font-size: 3.4rem;
  line-height: 1.1;
  margin-bottom: 1.5rem;
  color: var(--text-main);
}

h1 .accent {
  color: var(--pink-main);
  margin-left: 0.4rem;
}

p {
  font-size: 1.15rem;
  line-height: 1.7;
  color: var(--text-muted);
  max-width: 520px;
}

/* Photo */
.photo {
  position: relative;
  animation: fadeInRight 0.8s ease-out forwards;
}

.photo img {
  width: 100%;
  max-width: 380px;
  aspect-ratio: 1 / 1;
  object-fit: cover;
  border-radius: 50%;
}

/* Decorative accent */
.photo::before {
  content: '';
  position: absolute;
  inset: -20px;
  border-radius: 50%;
  background: radial-gradient(
    circle,
    var(--pink-main) 0%,
    transparent 70%
  );
  opacity: 0.25;
  z-index: -1;
}

/* Animations */
@keyframes fadeInLeft {
  from {
    opacity: 0;
    transform: translateX(-40px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes fadeInRight {
  from {
    opacity: 0;
    transform: translateX(40px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* Responsive */
@media (max-width: 900px) {
  .container {
    grid-template-columns: 1fr;
    text-align: center;
  }

  p {
    margin: 0 auto;
  }

  .photo {
    margin-top: 3rem;
  }
}
</style>
